namespace Colibri.Resources.DataTemplates
{
    //Helper class to make compiled bindings from resource dictionaries work
    //Compiled bindings requires code behind class so here it is
    internal partial class ConversationDataTemplates
    {
        public ConversationDataTemplates()
        {
            InitializeComponent();
        }
    }
}
